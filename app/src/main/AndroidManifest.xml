<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.arvin.nepaltouristguide">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".cafe.CafeActivity"></activity>
        <activity android:name=".cafe_detail.CafeDetailActivity"></activity>
        <activity android:name=".restaurant_detail.RestaurantDetailActivity" />
        <activity android:name=".home.VisitNepalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".place_options.PlacesOptionsActivity"
            android:label="@string/pokhara"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".camping.CampingActivity" />
        <activity android:name=".restaurant.RestaurantActivity" />
        <activity android:name=".cash_machine.CashMachineActivity" />
        <activity android:name=".mountain.MountainActivity" />
    </application>

</manifest>